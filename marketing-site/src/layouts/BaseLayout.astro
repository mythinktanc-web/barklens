---
import Footer from '@components/Footer.astro';
import Header from '@components/Header.astro';
import { site } from '@config/site';
import '@styles/global.css';

interface Props {
  title: string;
  description?: string;
  canonicalPath?: string;
  noindex?: boolean;
  image?: string;
  type?: 'website' | 'article';
  structuredData?: Record<string, unknown> | Record<string, unknown>[];
  compactFooter?: boolean;
  hideFooterOnMobile?: boolean;
}

const productionIndexingEnabled =
  import.meta.env.PUBLIC_SITE_NOINDEX === 'false' ||
  process.env.VERCEL_ENV === 'production';
const {
  title,
  description = site.description,
  canonicalPath = Astro.url.pathname,
  noindex = !productionIndexingEnabled,
  image = '/images/desktop-hero.webp',
  type = 'website',
  structuredData,
  compactFooter = false,
  hideFooterOnMobile = false
} = Astro.props;
const canonical = new URL(canonicalPath, site.canonicalOrigin);
const socialImage = new URL(image, site.canonicalOrigin);
const analyticsEnabled = import.meta.env.PUBLIC_ANALYTICS_ENABLED === 'true';
const analyticsId = String(import.meta.env.PUBLIC_GA_MEASUREMENT_ID || '').trim();
const organizationSchema = {
  '@context': 'https://schema.org',
  '@type': 'Organization',
  name: site.name,
  legalName: site.legalName,
  url: site.canonicalOrigin,
  email: site.contactEmail,
  logo: new URL('/favicon.svg', site.canonicalOrigin).href,
  sameAs: site.socialProfiles
};
const schemas = structuredData
  ? [organizationSchema, ...(Array.isArray(structuredData) ? structuredData : [structuredData])]
  : [organizationSchema];
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content={description} />
    <link rel="canonical" href={canonical} />
    <link rel="icon" href="/favicon.svg" type="image/svg+xml" />
    <meta name="robots" content={noindex ? 'noindex, nofollow' : 'index, follow, max-image-preview:large'} />
    <meta name="theme-color" content="#0f1f1a" />
    <meta property="og:site_name" content={site.name} />
    <meta property="og:type" content={type} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonical} />
    <meta property="og:image" content={socialImage} />
    <meta property="og:image:alt" content="BarkLens" />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={socialImage} />
    <title>{title}</title>
    <script is:inline type="application/ld+json" set:html={JSON.stringify(schemas)} />
    {analyticsEnabled && analyticsId && (
      <>
        <script is:inline async src={`https://www.googletagmanager.com/gtag/js?id=${encodeURIComponent(analyticsId)}`}></script>
        <script
          is:inline
          set:html={`window.dataLayer=window.dataLayer||[];function gtag(){dataLayer.push(arguments);}gtag('js',new Date());gtag('config',${JSON.stringify(analyticsId)},{anonymize_ip:true});`}
        />
      </>
    )}
  </head>
  <body>
    <a class="skip-link" href="#main-content">Skip to content</a>
    <Header />
    <main id="main-content">
      <slot />
    </main>
    <Footer compact={compactFooter} hideOnMobile={hideFooterOnMobile} />
  </body>
</html>

<a class="back-to-why" href="/#why-barklens" aria-label="Back to Why BarkLens">
  <svg width="12" height="24" viewBox="0 0 12 24" fill="none" aria-hidden="true" focusable="false">
    <path d="m7 6-6 6 6 6" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" />
  </svg>
</a>

<style>
  .back-to-why {
    display: inline-flex;
    align-items: center;
    /* Site rule: the visible chevron aligns with the heading's content gutter,
       not the center of its larger accessible tap target. */
    justify-content: flex-start;
    width: 2.75rem;
    min-height: 2.75rem;
    margin-bottom: 1.25rem;
    color: var(--color-green-600);
    border-radius: var(--radius-pill);
  }

  .back-to-why:hover {
    background: rgb(46 138 110 / 10%);
  }

  .back-to-why:focus-visible {
    outline: 2px solid currentColor;
    outline-offset: 3px;
  }
</style>
